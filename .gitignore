# Chat history working area
# Only a final, reviewed version gets committed on purpose (see CLAUDE.md).
chat-history.md
chat-history/

# .NET
bin/
obj/
*.user

# SQLite runtime database (created by demo/ScoreboardCli via Persistence/ScoreboardDbContext)
scoreboard.db
scoreboard.db-shm
scoreboard.db-wal

# OS
.DS_Store
